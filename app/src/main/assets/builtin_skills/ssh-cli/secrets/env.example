# SSH CLI secrets (DO NOT COMMIT REAL VALUES)
#
# 连接默认值（可选，仅用于 App 内 SSH 界面预填；ssh exec 仍建议显式 --host/--user）
SSH_HOST=
SSH_PORT=22
SSH_USER=
#
# 认证（二选一）
SSH_PASSWORD=
SSH_PRIVATE_KEY_PATH=skills/ssh-cli/secrets/id_ed25519
SSH_PRIVATE_KEY_PASSPHRASE=
#
# known_hosts 存储（可选，默认 workspace/ssh/known_hosts）
SSH_KNOWN_HOSTS_PATH=workspace/ssh/known_hosts
