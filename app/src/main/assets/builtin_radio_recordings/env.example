# ===== 阿里云百炼 ASR 配置（Radio 离线转录 / 翻译）=====
#
# 使用方式：
# - 在 App 的 Files 页签中打开：workspace/radio_recordings/.env
# - 填好 DASHSCOPE_API_KEY 后保存即可（不会通过 Settings 配置）
#
# 获取方式：https://help.aliyun.com/zh/model-studio/get-api-key

# API Key（必填）
DASHSCOPE_API_KEY=

# DashScope API Base URL（默认值，一般不需要修改）
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/api/v1

# ASR 模型名称（默认值，录音文件转写专用模型）
ASR_MODEL=qwen3-asr-flash-filetrans

